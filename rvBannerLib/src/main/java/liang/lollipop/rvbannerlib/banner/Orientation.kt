package liang.lollipop.rvbannerlib.banner

/**
 * 方向的枚举类
 * 用于控制Banner的滚动方向
 * @author Lollipop
 */
enum class Orientation(val value: Int) {

    /**
     * 横向滚动
     */
    HORIZONTAL(0),
    /**
     * 纵向滚动
     */
    VERTICAL(1)

}